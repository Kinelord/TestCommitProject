version: "3"services:  postgres:    container_name: postgres    image: postgres:14.6    #    restart: always  Уберите комментарий, если нужно чтоб Docker пытался загрузить контейнер, даже после перезагрузки    user: root    env_file: pstg_file_docker.env    volumes:      - ./database/postgres/data:/var/lib/postgresql/data      - ./postgres:/data/postgres    ports:      - "5433:5432"  pgadmin:    container_name: pgadmin    image: dpage/pgadmin4    environment:      PGADMIN_DEFAULT_EMAIL: igorshakirov01101996@gmail.com      PGADMIN_DEFAULT_PASSWORD: postgres    ports:      - "5052:80"# Для подключения к pgadmin, нужно перейти по адресу http://localhost:5052,# Чтобы подключиться к серверу, запущенной на Docker нужно ввести адрес подключения# host.docker.internal, а порт выбрать из ports: 5433# host.docker.internal 127.0.0.1# Для убийства залагавшего докера# wsl --shutdown# docker rm -f $(docker ps | grep 5444) Удалить образ который занимает порт 5444, ни смотря ни на что  rabbitmq:    image: rabbitmq:3.11-management    container_name: 'rabbitmq'    hostname: rabbitmq    environment:      - RABBITMQ_DEFAULT_USER=admin      - RABBITMQ_DEFAULT_PASS=admin      - RABBITMQ_SERVER_ADDITIONAL_ERL_ARGS=-rabbit log_levels [{connection,error},{default,error}] disk_free_limit 2147483648    volumes:      - ~/.docker-conf/rabbitmq/data/:/var/lib/rabbitmq/      - ~/.docker-conf/rabbitmq/log/:/var/log/rabbitmq    ports:      - "5674:5672"      - "15671:15672"